kind = "web"
previewPath = "/miniapp/"
title = "Finance Mini App"
version = "1.0.0"
id = "artifacts/miniapp"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/miniapp/" ]
localPort = 20220

[services.development]
run = "pnpm --filter @workspace/miniapp run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/miniapp", "run", "build" ]
publicDir = "artifacts/miniapp/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20220"
BASE_PATH = "/miniapp/"
